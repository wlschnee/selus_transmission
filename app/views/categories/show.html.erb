<div class="col-md-8">
  <h2><%= @category.name %></h2>
  <ul class="list list-unstyled">
    <% @category.posts.each do |post| %>
      <%= render partial: 'posts/post_container', locals: {post: post} %>
    <% end %>
  </ul>
</div>
<div class="col-md-4">
  <%= render partial: 'layouts/sidebar' %>
</div>